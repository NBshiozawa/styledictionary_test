{
  "devDependencies": {
    "style-dictionary": "^4.3.0",
    "token-transformer": "^0.0.33"
  },
  "type": "module",
  "scripts": {
    "transform": "token-transformer tokens/tokens.json tokens/sd-tokens.json",
    "build": "node build.js"
  }
}
